<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wmm.mapper.StudentMapper">


    <resultMap id="studentResultMap" type="com.wmm.pojo.Student">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="no" property="no"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="id_card" property="idCard"/>
        <result column="is_college" property="isCollege"/>
        <result column="address" property="address"/>
        <result column="degree" property="degree"/>
        <result column="graduation_date" property="graduationDate"/>
        <result column="clazz_id" property="clazzId"/>
        <result column="violation_count" property="violationCount"/>
        <result column="violation_score" property="violationScore"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="clazz_name" property="clazzName"/>
    </resultMap>



    <insert id="insert">

        insert into student(name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, create_time, update_time)
        value (#{name}, #{no}, #{gender}, #{phone}, #{idCard}, #{isCollege}, #{address}, #{degree}, #{graduationDate}, #{clazzId}, #{createTime}, #{updateTime});
    </insert>
    <update id="update">
        update student
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="no != null">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="isCollege != null">is_college = #{isCollege},</if>
            <if test="address != null">address = #{address},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            <if test="violationCount != null">violation_count = #{violationCount},</if>
            <if test="violationScore != null">violation_score = #{violationScore},</if>
        </set>
        where id = #{id};
    </update>
    <update id="updateViolation">
        update student set
                violation_count = violation_count + 1,
                violation_score = violation_score + #{score}
        where id = #{id};
    </update>

    <delete id="deleteById">
        delete from student where id = #{id};
    </delete>

    <select id="list" resultMap="studentResultMap">
        select s.id,s.name, s.no, s.gender, s.phone, s.id_card, s.is_college, s.address, s.degree,
        s.graduation_date, s.clazz_id, s.violation_count, s.violation_score, s.create_time, s.update_time, c.name as clazz_name
        from student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
        order by s.update_time desc
    </select>

    <select id="findById" resultMap="studentResultMap">
        select s.id, s.name, s.no, s.gender, s.phone, s.id_card, s.is_college, s.address, s.degree,
               s.graduation_date, s.clazz_id, s.violation_count, s.violation_score, s.create_time, s.update_time, c.name as clazz_name
        from student s left join clazz c on s.clazz_id = c.id
        where s.id = #{id}
    </select>

    <select id="countStudentDegreeData" resultType="java.util.Map">
        select
            case when degree=1 then '初中'
                 when degree=2 then '高中'
                 when degree=3 then '大专'
                 when degree=4 then '本科'
                 when degree=5 then '硕士'
                 else '其他' end as name,
            count(*) as value
        from student group by degree order by value desc;
    </select>
    <select id="countStudentCountData" resultType="java.util.Map">
        select c.name as name , count(*) as value from student
        left join clazz c on student.clazz_id = c.id
        group by c.name
        order by value desc
    </select>
</mapper>