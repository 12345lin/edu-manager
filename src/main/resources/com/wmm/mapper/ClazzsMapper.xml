<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wmm.mapper.ClazzsMapper">
    <update id="update">
        update clazz
        <set>
        <if test="name != null and name != ''"> name = #{name}, </if>
        <if test="room != null and room != ''"> room = #{room}, </if>
        <if test="beginDate != null"> begin_date = #{beginDate}, </if>
        <if test="endDate != null"> end_date = #{endDate}, </if>
        <if test="masterId != null"> master_id = #{masterId}, </if>
        <if test="subject != null and subject != ''"> subject = #{subject}, </if>
        <if test="updateTime != null"> update_time = #{updateTime}, </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM clazz WHERE id = #{id}
    </delete>


    <select id="list" resultType="com.wmm.pojo.Clazz">
        SELECT
        c.id, c.name, c.room, c.begin_date, c.end_date,
        c.master_id, c.subject, c.create_time, c.update_time,
        e.name as master_name
        FROM clazz c
        LEFT JOIN emp e ON e.id = c.master_id
        <where>
            <if test="name != null and name != ''"> c.name LIKE CONCAT('%', #{name}, '%') </if>
            <if test="begin_date != null"> AND c.begin_date = #{begin_date} </if>
            <if test="end_date != null"> AND c.end_date = #{end_date} </if>
        </where>  order by c.update_time desc
    </select>
    <select id="findById" resultType="com.wmm.pojo.Clazz">
        SELECT
        c.id, c.name, c.room, c.begin_date, c.end_date,
        c.master_id, c.subject, c.create_time, c.update_time,
        e.name as master_name
        FROM clazz c
        LEFT JOIN emp e ON e.id = c.master_id
        WHERE c.id = #{id}
    </select>

    <select id="listAll" resultType="com.wmm.pojo.Clazz">
        select id, name from clazz order by update_time desc
    </select>
    <select id="findIdByName" resultType="java.lang.Integer">
        select id from clazz where name = #{name}
    </select>
</mapper>